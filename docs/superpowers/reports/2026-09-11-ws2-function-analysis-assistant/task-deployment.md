# WS-2 도우미와 멘토 결과보고서 운영 배포 결과.

## RESULT.

- 2026-09-11 02:01 KST에 Vercel 운영 배포가 Ready 상태가 되었고 운영 도메인 연결을 확인했다.
- 운영 주소는 https://codetest2-rouge.vercel.app 이다. 이전 고정 Preview 주소는 이전 코드를 유지하므로 운영 주소로 확인한다.
- WS-2 기능분석 작성 도우미와 멘토 결과보고서 기능을 배포했다. 결과보고서의 초안·완료본 저장을 위한 운영 DB 마이그레이션도 적용했다.

## COMMIT.

- 배포 소스는 `e1651f87acaff88f8a6a0997a67758ab0fba0c02`이며 브랜치는 `codex/mentee-mentor-approval`이다.
- 최신 운영 소스 `origin/main`의 `68cba98`을 작업 브랜치에 병합한 커밋은 `ebbcf73`이다. WS-4 입력, QFD 동기화·권한, 서버 Word 생성 변경을 보존했다.
- `e1651f8`은 AI 제공자 폴백 테스트의 실제 DB 연결 의존성을 제거한 테스트 전용 변경이다. 제품 로직과 기존 단언·시간 제한은 변경하지 않았다.
- 작업 브랜치 푸시는 완료했다. `main` 브랜치 푸시는 수행하지 않았다. 이 보고서는 코드와 별도로 커밋한다.
- Preview 검증 대상은 `haDhrpm6gbLbWip1a4g6UE9UbCr5`이다.
- 운영 배포 ID는 `5HQ9ze5rqxDKjumeFctbYA5p6pzi`이며 Vercel의 Production·Current·Ready 상태와 위 소스 SHA를 확인했다.
- 운영 고정 배포 주소는 https://codetest2-fk6w4zx2e-cavinseos-projects.vercel.app 이다.

## VERIFIED BY.

### 통합 코드 검수.

- 루트 작업자가 `npm run test`를 실행하여 138개 파일, 1,775개 테스트 통과를 확인했다.
- `npm run lint`는 종료 코드 0이다. 기존 `.stryker-tmp` 파일의 경고 18개가 있으며 오류는 없다.
- 최종 `npm run build`는 종료 코드 0이며 정적 페이지 35/35개 생성에 성공했다. Vercel 운영 빌드도 1분 33초 후 Ready가 되었다.
- `git diff --check`를 통과했다. 기존 mutation 대상 파일은 통합 변경에 포함되지 않았다.
- 보고서 페이지 병합 시 화면에서 선택한 `shownDocument`를 서버 DOCX API로 보내도록 확인했다. 멘티가 이전 완료본을 보고 있을 때 새 초안이 다운로드되지 않도록 보존했다.

### 운영 DB 검수.

- 설정된 codetest2 Supabase 프로젝트 식별자를 확인한 후 `20260911000000_final_report_publication`을 적용했다. 다른 연결 프로젝트는 사용하지 않았다.
- 적용 전 기존 15개 마이그레이션 완료·신규 1개 대기·대상 테이블 부재를 확인했다. 적용 후 16개 모두 완료 상태다.
- `final_reports`의 9개 컬럼, 기본키, 프로젝트 외래키와 CASCADE, RLS 활성화, 공개 허용 정책 0개, 서버 역할 접근을 확인했다.
- 마이그레이션은 테이블 추가이며 기존 프로젝트·워크시트 행을 갱신하거나 삭제하지 않았다.

### 격리 환경의 완료본 다운로드 검수.

- 두 PostgreSQL 환경변수를 localhost의 전용 DB로 지정하고 최종 빌드를 실행했다.
- 초안 B와 이전 완료본 A를 서로 다른 문구로 저장한 검수 프로젝트에서 멘티가 A만 보고 편집할 수 없음을 확인했다.
- 브라우저의 Word 버튼 클릭 후 다운로드 성공 안내를 확인했다. 브라우저 도구의 blob 다운로드 이벤트는 관찰되지 않아 실제 응답 바이트를 별도로 검증했다.
- 동일 멘티의 보고서 GET 및 DOCX POST가 각각 200이고, DOCX MIME·ZIP 구조가 유효하며 `word/document.xml`에 A만 있고 B는 없음을 확인했다.
- 검수용 프로젝트·프로그램·사용자를 제거하고 작업 전용 PostgreSQL을 종료했다. 중간 앱 재시작으로 중단된 로컬 환경을 복구한 후 정리를 완료했다.

### 운영 브라우저 검수.

- 운영 프로젝트의 통합 WS-2에서 ‘기능분석 작성 도우미’ 버튼과 질문 팝업을 확인했다. 프로젝트명·설명이 자동 입력되었다.
- 검수용 답변으로 세 질문 단계를 이동하고 현재 WS-2 포함을 해제한 후 프롬프트를 생성했다.
- 생성 결과에 `FAST-WS2-1.0`과 빈 `existingSpecRows`가 있으며 기존 적용기술 문구는 제외됨을 확인했다. 복사 성공 안내를 확인했다.
- 운영 결과보고서에 관리자의 열람 화면, 배정 멘토만 작성·완료 가능 안내, 저장된 초안이 없다는 상태가 정상 표시되었다.
- 해당 브라우저에서 오류·경고 로그는 없었다. 운영 보고서 완료 처리나 워크시트 저장, 외부 AI 실행은 하지 않았다.

## DEVIATIONS.

- 배포 전 최신 main 변경을 발견해 작업 브랜치에 병합하고 검증했다.
- 전체 테스트 중 AI 폴백 테스트 2개가 실제 설정 DB 조회로 시간 초과되어 설정 조회만 대역으로 고정했다. 이후 관련 17개 및 전체 1,775개 테스트가 통과했다.
- 최초 로컬 빌드는 컴파일·페이지 생성 후 Windows `.next/export`의 일시적인 EBUSY 오류가 발생했다. 소스 변경이나 다른 작업 프로세스 종료 없이 재실행하여 성공했다.

## RISKS.

- Claude·OpenAI에서 분석을 실제 실행한 검수는 아니다. 생성 프롬프트의 형식·규칙과 UI 동작을 검증했으며 모델별 분석 내용 일치까지 보장하지 않는다.
- 멘토 작성·완료·멘티 다운로드의 데이터 변경 검수는 격리 DB에서 수행했다. 운영에서는 기존 자료를 조회하고 팝업 동작을 확인했다.
- 이전 앱 버전으로 복귀하더라도 새 보고서 자료 보존을 위해 추가 테이블과 마이그레이션 이력은 유지해야 한다.

## QUESTIONS.

- 배포에 남은 확인 사항은 없다. 사용자가 별도로 문의한 Codex 앱 종료·작업 기록 표시 문제는 앱 진단으로 분리해 확인한다.
