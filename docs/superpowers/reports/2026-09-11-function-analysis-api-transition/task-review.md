# 기능분석 API 전환 사전 검토 결과.

## RESULT.

- 사용자가 확정한 ‘멘티 계정 전체 누적 5회’와 당분간 프롬프트 제공 방식 유지 요구를 전환 설계에 반영했다.
- 공통 프롬프트 생성기가 이미 UI·DB·외부 호출에서 분리되어 있어 지금 실행 코드나 스키마를 바꿀 필요가 없다고 판단했다.
- 기존 JSON 전용 출력·규칙 기반 대체·무상태 브라우저 완료 경로를 새 HTML 분석과 횟수 차감에 그대로 재사용하지 않도록 경계를 기록했다.
- 서버의 계정별 실행 예약, 중복 요청 방지, 결과 불확실 상태와 사용량 보존, HTML 검증·정제, 기존 API 우회 경로 차단 및 전환 검수 시나리오를 명시했다.

## FILES CHANGED.

- `docs/superpowers/plans/2026-09-11-function-analysis-api-transition.md`를 추가했다.
- 기존 WS-2 도우미 계획서에 전환 계획 링크와 미활성화 상태를 기록했다.

## COMMIT.

- 설계 문서 커밋은 `c4daf5e`이다. 이 검토 보고서는 별도 커밋한다.
- 이번 작업에서는 푸시·배포를 수행하지 않았다.

## VERIFIED BY.

- 검토 담당과 주 작업자가 기존 실행 API·완료 API·인증·계정 이동 및 삭제·Prisma 스키마·공급자 출력 계약을 읽기 전용으로 대조했다.
- 주 작업자가 확인한 대표 근거는 순수 생성기, 전송기의 JSON 강제·형식 오류 재호출, `runAiTask`의 규칙 대체, 브라우저 제출 결과의 무상태 처리, 프로그램 이동 시 동일 사용자 ID 유지 및 계정 실제 삭제다.
- 설계 초안 재검토에서 ‘전환 준비 완료’라는 표현을 구조 검토 완료로 고치고, 공급자의 결과 재조회 지원 여부를 조건으로 명시했다.
- `npm run test -- tests/function-analysis-prompt.test.ts`는 `Test Files 1 passed (1)`, `Tests 31 passed (31)`이었다. 사전 한글 인코딩 검사도 통과했다.
- `git diff --cached --check`를 통과했다. 앱·컴포넌트·공통 코드·스키마·테스트·의존성 파일의 변경은 없었다.
- 문서만 변경했으므로 전체 빌드나 DB 검증을 다시 실행하지 않았다.

## DEVIATIONS.

- 코드 변경 대신 설계와 검수 기준을 반영했다. 현재 생성기의 재사용 경계가 이미 적합하고 사용자가 API 활성화를 유보했으므로, 미래 실행 코드·테이블을 미리 만드는 것은 불필요하다고 판단했다.

## RISKS.

- API 실행과 누적 횟수 제한은 아직 구현·검증되지 않았다. 미래 적용 전에 문서의 동시 요청·중복 요청·오류 복구·HTML 및 Excel 실사용 검수를 수행해야 한다.
- 실패·취소·원격 결과 불확실 시 차감, 다른 역할의 실행, 계정 삭제 후 재가입, 공급자·모델·비용·보관 정책은 미확정이다.

## QUESTIONS.

- 현재 프롬프트 제공 기능을 유지하는 데 필요한 추가 결정은 없다. 미확정 정책은 사용자가 API 전환을 요청하는 시점에 확정한다.
