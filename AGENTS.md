# 수행 지침

## 작업 순서

- 기능 변경과 결함 수정은 `검토 -> 구현 -> 테스트` 순서로 수행한다.
- 검토 단계에서 요구사항, 기존 코드 흐름, 데이터 손실 위험, UX 영향, 테스트 공백을 확인한다.
- 하위 에이전트를 쓸 경우 검토와 테스트를 병렬로 맡기되, 검토 담당은 코드를 수정하지 않고 발견사항을 심각도 순으로 보고한다.
- 최종 통합과 검증 결과 요약은 주 작업자가 책임진다.

## 코딩 전 검토

- 가정은 숨기지 말고 명시한다.
- 해석이 여러 개인 요구사항은 임의로 고르지 말고 차이를 설명하거나 질문한다.
- 불명확한 부분이 있으면 구현을 멈추고 무엇이 혼란스러운지 구체적으로 밝힌다.
- 여러 단계 작업은 간단한 계획과 각 단계의 검증 방법을 먼저 정리한다.
- 작업을 검증 가능한 성공 기준으로 바꾸고, 기준이 약하면 필요한 맥락을 먼저 확인한다.

## 단순성 우선

- 요청받지 않은 기능, 추상화, 설정 옵션, 확장성을 추가하지 않는다.
- 한 번만 쓰는 코드를 위해 새 추상화를 만들지 않는다.
- 실제로 발생할 수 없는 경우를 위한 방어 코드나 오류 처리를 덧붙이지 않는다.
- 더 단순한 해법이 있으면 먼저 제안하고, 구현이 과하게 길어지면 줄일 수 있는지 다시 검토한다.

## 좁고 안전한 변경

- 요청과 직접 관련된 파일과 줄만 수정하고, 모든 변경 라인은 요청과 직접 연결되어야 한다.
- 주변 코드, 주석, 포맷을 임의로 개선하지 않는다.
- 개인 선호와 달라도 프로젝트 기존 스타일을 따른다.
- 기존 사용자 변경 사항을 되돌리지 않는다.
- 내가 만든 변경 때문에 불필요해진 import, 변수, 함수만 정리한다.
- 그 밖의 죽은 코드나 개선점은 삭제하지 말고 보고한다. 단, 사용자가 정리를 요청하면 삭제한다.

## 테스트와 오류 처리

- 버그 수정은 가능하면 재현 테스트를 먼저 만들고 통과시키는 방식으로 진행한다.
- 코드를 수정했다면 완료 보고 전에 관련 테스트를 실행한다.
- 변경 범위가 넓거나 타입/빌드 영향이 있으면 `npm run lint`, `npm run test`, `npm run build`까지 확인한다.
- 실패하면 추측하지 말고 전체 오류 메시지와 스택 트레이스를 근거로 원인을 판단한다. 원인이 불명확하면 관찰 지점을 추가한다.
- 테스트를 실행할 수 없으면 그 이유와 남은 리스크를 보고한다.

## 새 소스 파일 헤더

- 새 소스 파일 첫 줄에 파일 역할을 설명하는 한국어 한 줄 주석을 둔다.
- 형식은 TS/JS `// 사용자 인증 상태를 관리하는 Context Provider`, Python `# KIS API 호출을 비동기로 래핑하는 클라이언트`, SQL `-- 일별 집계 결과를 저장하는 머티리얼라이즈드 뷰`.
- `'use client'`, `'use server'`, shebang 같은 필수 지시문이 있으면 그 바로 아래에 둔다.
- `*.config.ts`, `package.json` 같은 설정 파일에는 적용하지 않는다.

## 한국어 출력 규칙

- 사용자가 한국어로 요청하면 한국어로 답한다.
- 한국어 문장은 마침표, 물음표, 느낌표로 끝내고 콜론으로 끝내지 않는다. 콜론은 코드, 키-값 쌍, 라벨 내부에서만 쓴다.

## 커밋

- 하나의 논리적 변경이 완료되고 검증되면 의미 있는 단위로 커밋하는 것을 선호한다.
- 서로 다른 목적의 변경이나 관련 없는 기존 작업물을 한 커밋에 섞지 않는다.
- 커밋 여부가 애매하면 먼저 확인한다.
